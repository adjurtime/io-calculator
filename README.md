# IO Calculator

浏览器端投入产出（Input–Output, IO）分析工具，用于导入单区域 IO（SRIO）或按区域连续排列的多区域 IO（MRIO）矩阵，完成数据校验、系数与逆矩阵计算、卫星账户扩展、产业关联分析及结果导出。

当前版本：`1.0.0`

> English summary: A client-side IO analysis prototype for matrix validation, Leontief/Ghosh calculations, satellite-account multipliers, sector aggregation, and local result export.

## 当前状态

当前 v1 是教学和探索性研究原型，不应直接作为正式论文结果的唯一计算依据。

- 内置三部门示例可在开发服务器中完成“输入—校验—计算—结果”流程。
- 当前 `npm run build` 因 TypeScript 错误无法通过。
- 仓库已配置 Vitest，但尚无测试文件。
- 部分输入解析、MRIO 汇总、足迹结果维度和数值稳定性问题仍待修复。

正式研究中应使用独立程序复核关键矩阵、口径和结果。计划中的 v2 将优先处理可信计算、测试和 MRIO 可用性，而不是继续堆叠功能。

## 已实现功能

- 四步工作流：数据输入 → 数据校验 → 指标计算 → 结果导出。
- 内置三部门示例数据。
- Excel、CSV、TXT、DAT 和有限 MAT 文件导入。
- 从 Excel 或文本按数据类型逐项粘贴。
- Excel 多 Sheet 手动映射到 `Z`、`x`、`Y`、`VA`、`F`。
- 维度、零产出、负值和会计恒等式校验。
- SRIO 与手动 MRIO 区域配置。
- 连续等量合并或按部门编号自定义合并。
- 直接消耗系数、分配系数、Leontief 逆和 Ghosh 逆。
- 增加值系数、投入率、增加值率及乘数。
- 卫星强度、足迹乘数和最终需求足迹。
- 前向/后向产业关联及关键产业识别。
- 矩阵表格和热力图。
- Excel 与 JSON 下载。
- WIOD、EXIOBASE、GTAP、OECD ICIO、EORA、CEADs 等数据库的外部导航链接。

数据库链接只用于导航；应用不会自动下载或转换这些数据库的数据。

## 快速开始

### 环境要求

- Node.js 18 或更高版本；
- npm；
- 支持 ES2020、File API 和 Blob 下载的现代浏览器。

### 本地开发

```bash
git clone https://github.com/adjurtime/io-calculator.git
cd io-calculator
npm ci
npm run dev
```

开发服务器默认尝试使用 `http://localhost:5173`，并自动打开浏览器。

### 构建与预览

```bash
npm run build
npm run preview
```

`npm run build` 会先执行 TypeScript 检查，再由 Vite 构建到 `dist/`。当前 v1 基线尚不能通过这一步；请先查看“当前状态”和“已知限制”。

### 测试

```bash
npm test -- --run
```

当前命令会提示未找到测试文件，不能视为已有自动化测试覆盖。

## 推荐使用流程

1. 点击“加载示例”熟悉界面，或上传自己的数据。
2. 至少提供 `Z` 和真实的 `x`；按需补充 `Y`、`VA` 和 `F`。
3. 如使用 MRIO，设置区域数、每区域部门数和区域名称。
4. 进入数据校验，检查维度、会计平衡和所有警告。
5. 如需降低维度，在校验页执行部门合并。
6. 选择指标和容差，运行计算。
7. 在表格或热力图中检查结果。
8. 下载 Excel 或 JSON，并用独立程序复核关键结果。

页面刷新或点击“重置”会清除当前状态。应用目前没有项目保存、自动恢复或撤销功能。

## 数据模型与基本假设

令总部门数为 $n$。

| 数据 | 是否必需 | 维度 | 含义 |
|---|---:|---:|---|
| `Z` | 是 | $n\times n$ | 中间投入矩阵，$z_{ij}$ 表示部门 $i$ 向部门 $j$ 提供的中间产品 |
| `x` | 是 | $n$ | 各部门总产出 |
| `Y` | 否 | $n\times k$ | 最终需求矩阵，$k$ 为最终需求类别数 |
| `VA` | 否 | $m\times n$ | 增加值分项 |
| `F` | 否 | $p\times n$ | 直接卫星账户，如碳排放、能源或用水 |
| 部门名称 | 否 | $n$ | 未提供时使用“部门1、部门2……” |
| 区域名称 | 否 | 区域数 | 用于 MRIO 区域汇总 |

数据使用者需要保证：

- `Z`、`x`、`Y` 和 `VA` 使用一致的货币单位、价格口径、年份和进口处理方法；
- `F` 与 `Z`、`x` 的部门顺序完全一致；
- 单元格只包含可解析的数值，不包含单位字符或千位分隔文本；
- 矩阵不存在缺列、错位或不等长的行；
- 币种、价格、通胀、单位和部门分类转换已在导入前完成。

### MRIO 排列假设

MRIO 数据必须按连续区域块排列：

```text
区域1-部门1
区域1-部门2
...
区域2-部门1
区域2-部门2
...
```

并满足：

```text
区域数 × 每区域部门数 = n
```

各区域应使用相同的部门结构。当前界面对不相等配置只在浏览器控制台给出警告，不会阻止继续计算。

MRIO 模式下的核心矩阵计算仍对完整 $n\times n$ 系统执行。区域页目前只按连续切片汇总总产出、增加值和增加值率，不计算区域间隐含碳净转移或双边责任分配。

## 导入方式

| 格式 | 当前行为与要求 |
|---|---|
| `.xlsx`、`.xls` | 读取全部 Sheet，由用户为每个 Sheet 指定数据类型；可提取部门、最终需求、增加值、卫星账户和区域名称 |
| `.csv` | 每个文件读取一个矩阵；当前实现固定跳过首行和首列，因此文件应包含标题行和行标签列 |
| `.txt`、`.dat` | 每个文件读取一个矩阵；自动识别 Tab、分号、逗号或空格分隔，不自动跳过标题 |
| `.mat` | 支持部分未压缩 MAT v5 数值矩阵；不支持 MAT v7.3/HDF5 和压缩数据元素，建议从 MATLAB 导出为 `-v6` |
| 粘贴 | 支持 Tab、逗号或空白分隔；一次写入一个数据类型，可选择是否跳过首行和首列标题 |

对于 CSV、TXT、DAT 和 MAT，应用根据文件名或变量名推断数据类型。建议使用：

```text
Z
x
Y
VA
F
```

名称匹配采用简单的包含判断，复杂名称可能被误判。导入后必须检查维度预览。

### 推荐 Excel 工作簿结构

建议每类数据单独使用一个 Sheet，并在导入弹窗中手动确认类型：

```text
Z
x
Y
VA
F
sectors   （可选）
regions   （可选）
```

当前 Excel 导入器对 `x` 的首列处理与其他矩阵不同。`x` Sheet 建议只放一个带表头的数值列，不要在同一 Sheet 前置部门名称。

### 重要解析限制

- 缺少 `x` 时，当前实现会自动设置 $x_i=2\sum_j z_{ij}$。这只是临时启发式估计，不是投入产出会计恒等式，不能用于正式研究。
- 无法解析为数字的单元格会被转换为 `0`，而不是报错。
- 粘贴文本中的空单元格可能被过滤并导致后续列左移。
- 带行名的 Excel `x` 表可能被误读为全零向量。

应在导入前清理缺失值、文本标记和合并单元格，并始终提供真实总产出。

## 当前计算方法

记 $\hat{x}=\operatorname{diag}(x)$。当 $x_j=0$ 或 $|x_j|<10^{-15}$ 时，当前实现将对应倒数设为 `0`。

### 基础系数

直接消耗系数：

$$
A=Z\hat{x}^{-1},\qquad a_{ij}=\frac{z_{ij}}{x_j}
$$

分配系数：

$$
B=\hat{x}^{-1}Z,\qquad b_{ij}=\frac{z_{ij}}{x_i}
$$

Leontief 与 Ghosh 逆：

$$
L=(I-A)^{-1},\qquad G=(I-B)^{-1}
$$

### 最终需求与卫星账户

当前界面固定把最终需求各列合并为：

$$
y=Y\mathbf{1}
$$

直接卫星强度与足迹乘数：

$$
s=F\hat{x}^{-1},\qquad M=sL
$$

合并最终需求后的卫星总量：

$$
q=My
$$

当前版本没有按最终需求类别分别计算的界面选项，多卫星账户的足迹导出维度也需要在 v2 修复。

### 产业关联

后向与前向关联：

$$
BL_j=\sum_i L_{ij},\qquad FL_i=\sum_j G_{ij}
$$

两者分别除以全部部门的算术平均值得到标准化系数。当标准化后向和前向关联系数均大于 1 时，界面把该部门标记为关键产业。

## 部门合并

校验没有错误时，可启用部门合并。

### 等量合并

每连续 $N$ 个部门合并为一个部门。`Z` 按新行组与列组求和，`x` 和 `Y` 按部门行求和，`VA` 与 `F` 按部门列求和。

MRIO 数据只有在 $N$ 能整齐适配每区域部门结构时才应使用等量合并，否则可能跨越区域边界。

### 自定义合并

规则使用 1-based 部门编号：

```text
15,20=部门15+20
22,23,24=制造业合计
```

未出现在规则中的部门保持不变。MRIO 模式下，编号表示区域内部门位置，同一组规则会应用到每个区域。

合并直接替换当前内存中的数据。当前没有撤销按钮，需要重新加载原始文件或重置应用。

## 结果展示与导出

矩阵 `A`、`L`、`G`、`s` 和 `M` 支持表格与热力图切换。向量、乘数、产业关联和区域汇总使用表格显示。

下载文件名形如：

```text
IO_Results_YYYYMMDD.xlsx
IO_Results_YYYYMMDD.json
```

当前页面、Excel 和 JSON 的指标覆盖并不完全一致：

| 内容 | 页面 | Excel | JSON |
|---|---:|---:|---:|
| 原始 `Z`、`x`、`Y`、`VA`、`F` | 仅维度摘要 | 是 | 否 |
| `A`、`L`、总增加值系数、乘数、`s`、`M` | 是 | 是 | 是 |
| `B` | 否 | 选择计算时导出 | 是 |
| `G` | 是 | 否 | 是 |
| 中间投入率、增加值率 | 否 | 是 | 是 |
| 最终需求足迹 | 否 | 是 | 是 |
| 产业关联与关键产业 | 是 | 否 | 是 |
| MRIO 区域汇总 | 是 | 否 | 否 |

代码中有单矩阵 CSV 导出函数，但当前页面没有对应按钮。

## 隐私、网络与安全

- 文件由浏览器读取并在当前页面内计算，源码中没有把输入矩阵上传到应用服务器的请求。
- 当前代码不使用 `localStorage`、`sessionStorage` 或 IndexedDB 持久化数据。
- 下载结果由浏览器本地生成。
- 页面启动时会请求 Google Fonts 和 jsDelivr 上的 ECharts；当前版本不是完全离线封装。
- 页脚数据库链接只有在用户点击后才会访问对应网站。
- 导入的 Sheet 名称、部门名称和区域名称会直接写入 HTML，未进行完整转义。只处理可信文件，不要把本工具作为不受信任文件的安全查看器。
- CSV 导出未防护以 `=`, `+`, `-`, `@` 开头的标签；打开不可信导出前应人工检查。

## 技术栈

- TypeScript
- Vite
- mathjs：矩阵乘法、行列式和逆矩阵
- SheetJS `xlsx`：Excel 读取与写出
- ECharts 5.4.3：通过 CDN 加载热力图
- 原生 DOM、File API 和 Blob 下载

`chart.js` 和 `handsontable` 已列入依赖，但当前源码没有导入它们。

## 已知限制

- 缺失 `x` 时的自动估计不能用于正式研究。
- 非数值内容会静默变成 `0`，缺失单元格可能造成错位。
- 校验没有完整拒绝 ragged、`NaN`、`Infinity`、病态系统和所有零产出关联流。
- 当前用绝对行列式阈值判断矩阵是否可逆，随矩阵维度和尺度变化，不适合作为稳定性判据。
- 足迹的配置依赖和多卫星结果形状存在错误；页面中的“部门足迹”实际上是合并最终需求后的卫星总量。
- MAT 支持范围有限，不支持 v7.3/HDF5 和压缩矩阵元素。
- MRIO 区域汇总只是连续切片示例，不是双边隐含流、贸易转移或消费责任分析。
- 未实现数据平衡、部门映射、单位转换、价格调整和数据库自动接入。
- 浏览器主线程显式求逆、完整渲染矩阵，无法保证常见大型 MRIO 的性能或数值稳定性。
- 计算失败主要写入控制台，页面仍可能进入结果页。
- 页面、Excel、JSON 三种输出的结果契约不一致。
- 上传区、步骤导航和模态框的键盘与屏幕阅读器支持不足。
- 当前依赖审计包含高危及严重级别告警；升级前需要兼容性和回归测试。
- 没有项目保存、撤销、版本管理、协作或自动化测试。

## v2 方向

v2 的建议边界是“可信的 SRIO 与中等规模 MRIO 浏览器工具”，而不是在浏览器中直接承载 GLORIA、EXIOBASE 等完整大型数据库。

优先级：

1. 建立黄金算例与导入、维度、足迹、聚合测试，恢复可构建基线。
2. 禁止虚构 `x`，严格报告空值和非数值单元格，结构错误立即停止计算。
3. 修复足迹维度、指标依赖和 MRIO 区域契约，统一页面与导出结果。
4. 用线性方程求解和条件估计替代行列式门槛及不必要的显式求逆。
5. 拆分界面状态和结果模块，增加 Web Worker、进度、取消、尺寸上限和表格虚拟化。
6. 清理未使用依赖与历史修复脚本，完成依赖升级、安全转义和可访问性修复。

大型 MRIO 应通过 Python/本地服务或桌面后端完成稀疏计算，浏览器负责配置、抽样预览和结果解释。

## 项目结构

```text
.
├── index.html
├── src/
│   ├── main.ts               # 单页界面、状态和交互流程
│   ├── index.css             # 深色主题与响应式样式
│   ├── types/io.ts           # 数据、配置、校验和结果类型
│   ├── core/
│   │   ├── matrix.ts         # 基础矩阵运算
│   │   ├── validation.ts     # 数据与会计恒等式校验
│   │   ├── calculator.ts     # IO 指标计算
│   │   └── aggregation.ts    # SRIO/MRIO 部门合并
│   └── utils/
│       ├── fileIO.ts         # 文件、粘贴数据和 MAT 解析
│       └── export.ts         # Excel、JSON 与内部 CSV 导出函数
├── package.json
├── package-lock.json
├── tsconfig.json
├── vite.config.ts
├── fix_ui.js
└── fix_ui.cjs
```

`fix_ui.js` 和 `fix_ui.cjs` 是带硬编码 Windows 路径的历史修复脚本，不属于应用运行或 npm 构建流程。

## 许可状态

当前仓库没有项目级 `LICENSE` 文件，`package.json` 也没有声明 `license` 字段，因此没有授予明确的开源使用、修改或再分发许可。

依赖包各自具有独立许可证。公开发布或商业使用前，应补充项目许可证并完成依赖许可证审查。
